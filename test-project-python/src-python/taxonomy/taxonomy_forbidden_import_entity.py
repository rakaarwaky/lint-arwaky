# AES001: taxonomy(entity) importing infrastructure (FORBIDDEN)
# taxonomy layer → only allowed to import taxonomy, NOT infrastructure
from infrastructure import infra_service_base


class TaxonomyForbiddenImportEntity:
    pass

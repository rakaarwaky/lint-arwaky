// AES305: duplicate code block
function computeValue(x: number): number {
  const result = x + 1;
  return result;
}
function anotherUtil(y: string): string {
  const result = y.toUpperCase();
  return result;
}

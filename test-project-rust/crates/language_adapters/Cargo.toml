[package]
name = "language-adapters"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/lib.rs"

[dependencies]
taxonomy = { path = "../taxonomy" }
layer-rules = { path = "../layer_rules" }
di-containers = { path = "../di_containers" }


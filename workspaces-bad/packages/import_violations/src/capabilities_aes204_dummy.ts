// Fixture: AES204 — dummy function to suppress unused import warning.
import { UserVO } from '../taxonomy/vo';

export function process() {
  const x = 42;
  console.log(`value: ${x}`);
}

function _useUserVo(_u: UserVO): void {}

thresholds:
  score: 100.0
  complexity: 15

adapters:
  - name: "ruff"
    weight: 1.0
  - name: "mypy"
    weight: 1.0
  - name: "bandit"
    weight: 1.0
  - name: "architecture"
    weight: 3.0

ignored_rules: []

ignored_paths:
  - "/.mimocode"
  - "./output"
  - "/.git"
  - "/.github"
  - "/scratch"
  - "/packages"
  - "/__pycache__"
  - "/node_modules"
  - "/.venv"
  - "/venv"
  - "/.mypy_cache"
  - "/.ruff_cache"
  - "/test-workspaces"
  - "/tests"

architecture:
  enabled: true
  rules:
    AES101:
      enabled: true
      scope:
        [
          "taxonomy",
          "contract",
          "capabilities",
          "agent",
          "surfaces",
          "root",
        ]
      exceptions: ["__init__.py", "__main__.py", "main.py"]

    AES102:
      enabled: true
      scope:
        [
          "taxonomy",
          "contract",
          "capabilities",
          "agent",
          "surfaces",
          "root",
        ]
      exceptions: ["__init__.py", "main.py", "lib.py"]
      layers:
        taxonomy:
          suffix:
            - strict: ["vo", "entity", "error", "event", "constant", "utility"]
        contract:
          suffix:
            - strict: ["port", "protocol", "aggregate"]
        capabilities:
          suffix:
            - flexible:
                [
                  "analyzer",
                  "checker",
                  "processor",
                  "evaluator",
                  "resolver",
                  "validator",
                  "formatter",
                  "executor",
                  "transformer",
                  "calculator",
                  "builder",
                  "compiler",
                  "aggregator",
                  "classifier",
                  "extractor",
                  "reporter",
                  "mapper",
                  "filter",
                  "collector",
                  "comparator",
                  "scorer",
                  "inspector",
                  "reviewer",
                  "assessor",
                  "auditor",
                  "actions",
                  "helper",
                ]
            - forbidden:
                [
                  "vo",
                  "entity",
                  "error",
                  "event",
                  "constant",
                  "constants",
                  "port",
                  "protocol",
                  "aggregate",
                  "utility",
                ]
        agent:
          suffix:
            - strict: ["orchestrator"]
        surfaces:
          suffix:
            - strict:
                [
                  "command",
                  "controller",
                  "page",
                  "view",
                  "component",
                  "router",
                  "layout",
                  "hook",
                  "store",
                  "action",
                  "screen",
                ]
        root:
          suffix:
            - strict: ["entry", "container"]

    AES201:
      enabled: true
      scope:
        [
          "taxonomy",
          "contract",
          "capabilities",
          "agent",
          "surfaces",
        ]
      exceptions: ["__init__.py", "__main__.py", "main.py"]
      conditions:
        - scope: "taxonomy(vo)"
          allowed: ["taxonomy"]
          mandatory: null
          forbidden:
            [
              "agent",
              "surfaces",
              "contract",
              "capabilities",
              "root",
            ]

        - scope: "taxonomy(entity,error,event)"
          allowed: ["taxonomy"]
          mandatory: ["taxonomy(vo|constant)"]
          forbidden:
            [
              "agent",
              "surfaces",
              "contract",
              "capabilities",
              "root",
            ]

        - scope: "taxonomy(constant)"
          allowed: ["taxonomy"]
          mandatory: null
          forbidden:
            [
              "agent",
              "surfaces",
              "contract",
              "capabilities",
              "root",
            ]

        - scope: "contract(port|protocol)"
          allowed: ["taxonomy", "contract"]
          mandatory: ["taxonomy"]
          forbidden:
            [
              "agent",
              "surfaces",
              "capabilities",
              "contract(aggregate)",
              "root",
            ]

        - scope: "contract(aggregate)"
          allowed: ["taxonomy", "contract"]
          mandatory: ["taxonomy"]
          forbidden:
            ["agent", "surfaces", "capabilities", "root"]
          forbidden_inheritance: ["contract(port)", "contract(protocol)"]

        - scope: "capabilities"
          allowed: ["taxonomy", "contract"]
          mandatory: ["taxonomy", "contract(protocol)"]
          forbidden:
            ["surfaces", "agent", "capabilities", "root"]
          allowed_imports:
            - layer: "taxonomy"
              purpose: "implement"
            - layer: "contract(port)"
              purpose: "implement"
            - layer: "contract(protocol|aggregate)"
              purpose: "call"

        - scope: "agent(orchestrator)"
          allowed:
            [
              "taxonomy",
              "contract(aggregate)",
              "contract(port)",
              "contract(protocol)",
            ]
          mandatory: ["taxonomy", "contract(aggregate)"]
          forbidden: ["surfaces", "capabilities", "root"]

        - scope: "surfaces(command|controller|page|)"
          allowed: ["taxonomy", "contract"]
          mandatory: null
          forbidden:
            [
              "agent",
              "capabilities",
              "contract(port)",
              "contract(protocol)",
              "root",
            ]

        - scope: "surfaces(hook|store|action|screen|router)"
          allowed: ["taxonomy"]
          mandatory: null
          forbidden:
            [
              "agent",
              "capabilities",
              "contract(port)",
              "contract(protocol)",
              "surfaces(command|controller|page|entry)",
              "root",
            ]

        - scope: "surfaces(component|view|layout)"
          allowed: ["taxonomy"]
          mandatory: null
          forbidden:
            [
              "agent",
              "contract",
              "capabilities",
              "surfaces(command|controller|page|entry|hook|store|action|screen|router)",
              "root",
            ]

    AES202:
      enabled: true
      scope:
        [
          "taxonomy",
          "contract",
          "capabilities",
          "agent",
          "surfaces",
        ]
      exceptions: ["__init__.py", "__main__.py", "main.py"]
      conditions:
        - scope: "capabilities"
          allowed: ["taxonomy", "contract"]
          mandatory: ["taxonomy", "contract(protocol)"]
          forbidden:
            ["surfaces", "agent", "capabilities", "root"]
          allowed_imports:
            - layer: "taxonomy"
              purpose: "implement"
            - layer: "contract(port)"
              purpose: "implement"
            - layer: "contract(protocol|aggregate)"
              purpose: "call"

    AES203:
      enabled: true
      scope:
        [
          "taxonomy",
          "contract",
          "capabilities",
          "agent",
          "surfaces",
        ]

    AES204:
      enabled: true
      scope:
        [
          "taxonomy",
          "contract",
          "capabilities",
          "agent",
          "surfaces",
        ]
      exceptions: []

    AES205:
      enabled: true
      scope:
        [
          "taxonomy",
          "contract",
          "capabilities",
          "agent",
          "surfaces",
        ]

    AES301:
      enabled: true
      scope:
        [
          "taxonomy",
          "contract",
          "capabilities",
          "agent",
          "surfaces",
        ]
      max_lines: 1000
      exceptions: ["__init__.py", "__main__.py", "main.py"]

    AES302:
      enabled: true
      scope:
        [
          "taxonomy",
          "contract",
          "capabilities",
          "agent",
          "surfaces",
        ]
      min_lines: 5
      exceptions: ["__init__.py", "__main__.py", "main.py"]

    AES303:
      enabled: true
      scope:
        [
          "taxonomy",
          "contract",
          "capabilities",
          "agent",
          "surfaces",
        ]
      exceptions: ["__init__.py", "__main__.py", "main.py"]

    AES304:
      enabled: true
      scope:
        [
          "taxonomy",
          "contract",
          "capabilities",
          "agent",
          "surfaces",
        ]
      forbidden_bypass:
        [
          "# noqa",
          "# type: ignore",
          "# fmt: off",
          "# ruff: noqa",
          "# pylint: disable",
          "Any",
          "except:",
          "pass",
        ]
      exceptions: []

    AES305:
      enabled: true
      scope:
        [
          "taxonomy",
          "contract",
          "capabilities",
          "agent",
          "surfaces",
        ]
      exceptions: []

    AES401:
      enabled: true
      scope:
        ["taxonomy(constant)", "taxonomy(entity,error,event)", "taxonomy(vo)"]

    AES402:
      enabled: true
      scope: ["contract(aggregate)", "contract(port|protocol)"]
      exceptions: ["__init__.py", "__main__.py", "main.py"]

    AES403:
      enabled: true
      scope: ["capabilities"]
      max_functions: 30
      max_impl: 5
      exceptions: ["__init__.py", "__main__.py", "main.py"]

    AES405:
      enabled: true
      scope: ["agent(orchestrator)"]
      max_lines: 1000
      exceptions: ["__init__.py", "__main__.py", "main.py"]

    AES406:
      enabled: true
      scope:
        [
          "surfaces(command|controller|page|entry)",
          "surfaces(hook|store|action|screen|router)",
          "surfaces(component|view|layout)",
        ]
      max_functions: 15
      exceptions: ["__init__.py", "__main__.py", "main.py"]

    AES501:
      enabled: true
      scope: ["taxonomy"]
      entry_points: []

    AES502:
      enabled: true
      scope: ["contract"]
      entry_points: []

    AES503:
      enabled: true
      scope: ["capabilities"]
      entry_points: []

    AES505:
      enabled: true
      scope: ["agent"]
      entry_points: []

    AES506:
      enabled: true
      scope: ["surfaces"]
      entry_points: []

[package]
name = "orphan_detector"
version = "1.10.11"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
regex = "1.10"
code_analysis = { path = "../code-analysis" }
output_report = { path = "../output-report" }
shared = { path = "../shared" }
source_parsing = { path = "../source-parsing" }

[package]
name = "config_system"
version = "1.10.11"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
async-trait = "0.1.89"
di_containers = { path = "../di-containers" }
import_rules = { path = "../import-rules" }
pipeline_jobs = { path = "../pipeline-jobs" }
project_setup = { path = "../project-setup" }
shared_common = { path = "../shared-common" }
source_parsing = { path = "../source-parsing" }

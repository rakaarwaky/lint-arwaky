[package]
name = "code_analysis"
version = "1.10.11"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
async-trait = "0.1.89"
once_cell = "1.21.4"
regex = "1.10"
auto_fix = { path = "../auto-fix" }
cli_commands = { path = "../cli-commands" }
config_system = { path = "../config-system" }
di_containers = { path = "../di-containers" }
import_rules = { path = "../import-rules" }
language_adapters = { path = "../language-adapters" }
output_report = { path = "../output-report" }
role_rules = { path = "../role-rules" }
shared_common = { path = "../shared-common" }
source_parsing = { path = "../source-parsing" }

[package]
name = "metrics_service"
version = "1.10.11"
edition = "2021"

[dependencies]
async-trait = "0.1.89"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
shared = { path = "../shared" }
source_parsing = { path = "../source-parsing" }

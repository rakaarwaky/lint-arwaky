[package]
name = "pipeline_jobs"
version = "1.10.11"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
async-trait = "0.1.89"
tokio = { version = "1.52.3", features = ["full"] }
chrono = "0.4"
rand = "0.8"
multi_project = { path = "../multi-project" }
shared = { path = "../shared" }
source_parsing = { path = "../source-parsing" }

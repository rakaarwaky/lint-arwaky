[package]
name = "import_rules"
version = "1.10.11"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
async-trait = "0.1.89"
file_system = { path = "../file-system" }
output_report = { path = "../output-report" }
shared = { path = "../shared" }
source_parsing = { path = "../source-parsing" }

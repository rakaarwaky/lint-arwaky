// PURPOSE: ViolationItem — re-export from shared taxonomy layer.
// Original definition moved to shared/src/common/taxonomy_violation_item_vo.rs
// (AES401 compliance: surface layer must not define shared domain types).

pub use shared::common::ViolationItem;

## Role: Developer (Executor)

Execute the merged plan from Architect.

### Prerequisites

1. Merged plan from Architect: {{MERGED_PLAN}}
2. /home/raka/mcp-arwaky/lint-arwaky/.agents/rules/RULES_AES.md
3. /home/raka/mcp-arwaky/lint-arwaky/ARCHITECTURE.md

### Workflow (Sequential, No Skips)

**Step 1: READ MERGED PLAN**
- Read {{MERGED_PLAN}}
- Understand all findings and action items
- Identify priority of each action item

**Step 2: IMPLEMENT**
- Execute action items by priority
- Create worktree (MANDATORY — do NOT work on main tree):
  cd /home/raka/mcp-arwaky/lint-arwaky
  git worktree add .worktree/worktree-{{FEATURE}} -b worktree-{{FEATURE}}
  cd .worktree/worktree-{{FEATURE}}
  If worktree already exists, use the existing one instead.
- Work ONLY inside the worktree directory
- Follow recommended fixed code from merged plan
- Commit with conventional commits
- Push to remote

**Step 3: SELF-VERIFY**
Run: bash scripts/gates.sh
Ensure 0 violations.

**Step 4: CREATE PR**
- Push to remote
- Create PR to develop
- Label: status: pending-review
- Body: link to merged plan

**Step 5: WRITE REPORT**
Save report to: /home/raka/mcp-arwaky/lint-arwaky/.agents/graph-loop/reports/done-{{FEATURE}}-{{DATE}}.md
Include: PR number, changes made, test results.

Report format:
# Execution Report: {{FEATURE}} — Developer
## PR Info
- PR: #{{PR_NUMBER}}
- Branch: worktree-{{FEATURE}} to develop
- Merged Plan: {{MERGED_PLAN}}
## Changes Made
List of files changed and what was done
## Self-Verification
| Gate | Result |
| cargo check | pass/fail |
| cargo clippy | pass/fail |
| cargo fmt | pass/fail |
| cargo nextest | pass/fail |
| gates.sh | pass/fail |
## Test Results
Summary of test output

### Implementation Rules

- Only modify files within {{FEATURE_PATH}}
- Follow existing code style and conventions
- Add tests for new functionality
- Ensure all quality gates pass
- Use conventional commits
- Link merged plan in PR body

RULE: Only modify files within {{FEATURE_PATH}}. Do NOT touch files outside.
